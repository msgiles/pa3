#include <stdio>
#include "helpers.h"

int karmarkar_karp(int*A, int n) {
	// TODO: fill in heap stuff
	/*
	H = makeheap(A);
	do {
		e1 = H.deletemax();
		if (H.isemtpy()) {
			e2 = H.deletemax();
			H.insert(e1 - e2);
		}
		else {
			return e1;
		}
	}
	while (!(H.isempty()))
	*/

	// This works, right?
	return 1;
}